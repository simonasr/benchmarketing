---
description: "Docker and deployment configuration patterns"
globs: "compose*.yaml,Dockerfile,*.conf"
---

# Docker and Deployment Patterns

## Docker Compose Configuration

### Service Configuration
- Use environment variables for all dynamic configuration
- Use meaningful service names that reflect their purpose
- Include configuration comments for different scenarios

### Volume Mounting Pattern
Use volumes for persistent data (e.g., Prometheus storage). Certificate mounts are no longer required.

### Target Configuration
Provide targets to the controller via job API payloads (not env vars):
```json
{
  "targets": [
    { "redisUrl": "redis://redis.example.com:6379", "workerCount": 2 },
    { "clusterUrl": "redis://cluster.example.com:6379", "workerCount": 2 }
  ]
}
```

## Certificate Management
Not applicable.

## Configuration Flexibility
Support deployment scenarios via controller job targets:
- **Single Instance**: `redisUrl=redis://host:port`
- **Cluster Mode**: `clusterUrl=redis://host:port`

## Documentation Standards
Include usage comments in Docker Compose files as needed.
