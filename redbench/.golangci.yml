run:
  go: '1.22'
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck 